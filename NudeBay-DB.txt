TODO:
-SPRAWDZIĆ POPRAWNOŚĆ OGRANICZEŃ DLA DŁUGOŚĆI WARTOŚCI W ARRAYACH ORAZ OGRANICZEŃ ILOŚĆ WARTOŚCI W ARRAYACH

//NudeBay-DB:

// Accounts
Users:
-nick (może się powtarzać)
-tag
-email (nie może się powtarzać)
-password
-badges{
    -role1name,
    -role2name,
    ...
}(array, many) (owner, admin, mod, verificated, pussypass) (default [])
-aboutMe (default '')
-status (default '')
-profilePicture (default null)
-backgroundPicture (default null)
-createDate (default new Date())
-devices (maybe)
-delete {
    -isDeleted
    -deleteDate
}
-bans [{
    -isBanned
    -banDate
    -banReason
    -banExpirationDate
}](array)
-following{
    -UserID,
    -User2ID,
    ...
}
-saved{
    -PostID,
    -Post2ID,
    ...
}
-settings{
    -dark mode
    -language
    -notifications {
        -newPost(boolean)
        -newFollower
        -newComment
        -newLike
        -newMessage
    }
}(array, many, each unique)
-notification [{
    -type
    -date
    -isRead
    -data
}]

// Posts
Posts:
-senderID
-content
-date
-views
-images{
    -Image1 or only one Video,
    -Image2,
    ...
} (array one-many, each unique)
-reactions{
    -UserID,
    -User2ID,
    ...
} (array one-many, each unique)
-comments{
    {
        -userID,
        -content
    },
    {
        -user2ID,
        -content
    }
} (array one-many)
-tags{
    -tagName,
    -tag2Name,
    ...
} (array one-many, each unique)
-users{
    -userID,
    -user2ID,
    ...
} (array one-many, each unique)

// Tags
Tags:
-name(each unique)

// Messages
Friendships: (and invites)
-senderID
-receiverID
-status (accepted, pending or blocked) (default pending) (no array bc only one)
-muted (true or false) (default false)

Messages:
-senderID
-receiverID
-content
-date

// Reports
Reports: