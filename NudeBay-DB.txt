TODO:
-WPROWADZIĆ OGRANICZENIA DLA DŁUGOŚĆI WARTOŚCI W ARRAYACH ORAZ WPROWADZIĆ OGRANICZENIA ILOŚĆ WARTOŚCI W ARRAYACH

//NudeBay-DB:

// Accounts
Users:
-nick (może się powtarzać)
-tag
-password
-roles{
    -role1name,
    -role2name,
    ...
}(array, many) (admin, mod, verificated, pussypass) (default null)
-email (nie może się powtarzać)
-aboutme (default null)
-status (default null)
-profilePicture (default null)
-createDate (default new Date())
-followed{
    -UserID,
    -User2ID,
    ...
}
-saved{
    -PostID,
    -Post2ID,
    ...
}
-settings{
    -setting1: value,
    -setting2: value,
    ...
}(array, many, each unique)

// Posts
Posts:
-senderID
-content
-images{
    -Image1,
    -Image2,
    ...
} (array one-many, each unique)
-reactions{
    -UserID,
    -User2ID,
    ...
} (array one-many, each unique)
-comments{
    {
        -userID,
        -content
    },
    {
        -user2ID,
        -content
    }
} (array one-many)
-tags{
    -tagName,
    -tag2Name,
    ...
} (array one-many, each unique)
-users{
    -userID,
    -user2ID,
    ...
} (array one-many, each unique)

// Tags
Tags:
-name(each unique)

// Messages
Friendships: (and invites)
-senderID
-receiverID
-status (accepted, pending or blocked) (default pending) (no array bc only one)
-muted (true or false) (default false)

Messages:
-senderID
-receiverID
-content